FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml .
RUN pip install -e ".[tui]" --no-cache-dir

COPY src/ ./src/

EXPOSE 8000

CMD ["python", "-m", "src.tui.main"]
